# Machine-local profile declarations — generated by install.sh or the
# new-profile skill, never versioned. Only *.conf.example ships in the repo.
profiles.d/*.conf

# Backups created by install.sh when it replaces an existing file
*.bak.*

# OS cruft
.DS_Store

shared/AGENTS.md
share.conf
